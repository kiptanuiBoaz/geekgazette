import { Hero, Read } from "../components/index";

const HomePage = () => {
  return (
    <main
      style={{
        margin: 0,
        padding: 0,
        width: "100%",
      }}>
      <Hero />
      <Read />
    </main>

  )
}

export default HomePage;
